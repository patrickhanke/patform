'use client';

import React, { useContext } from 'react';
import { ContextValues } from './types';

export const AdminProjectContext = React.createContext({} as ContextValues );

// export const useAdminProjectContext = useContext(AdminProjectContext);
