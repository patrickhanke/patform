import { ViewState } from '../types';

const viewSettings: ViewState[] = [
	{label: 'Monatsansicht', value: 'monthly', is_icon: false},
	{label: 'Quartalsansicht', value: 'quaterly', is_icon: false}
];

export default viewSettings;