{
  "name": "patform",
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo lint",
    "gen": "turbo gen",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@manypkg/cli": "^0.23.0",
    "syncpack": "^13.0.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@9.6.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "firebase": "^11.1.0",
    "idb": "^8.0.1"
  }
}
