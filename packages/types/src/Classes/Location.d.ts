import { MapPlace } from '@repo/ui';

export type LocationClass = {
    image: string;
    objectId: string;
    name: string;
    address: string;
    geopoint: MapPlace;
    address: string;
    description: string;
};