import { MenuItem, MenuItemProps } from "@repo/ui";

const static_menu_items: MenuItemProps[] = [
  {
    label: "Forms",
    value: "/forms",
    icon: "forms",
    divider: "Homepage",
    subMenu: [],
  },
];

export default static_menu_items;
