import { PageState } from '@repo/types';

const pages_states: PageState[] = [
	{
		label: 'Einstellungen',
		value: 'settings'
	},
	{
		label: 'Seiten',
		value: 'pages'
	}
];

export default pages_states;