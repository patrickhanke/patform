import { ClassState } from '@repo/types';

const state: ClassState = [
	{
		value: 'draft',
		label: 'Entwurf',
		color: '#FFD700'
	},
	{
		value: 'published',
		label: 'Veröffentlicht',
		color: '#00FF00'
	}
];

export default state;